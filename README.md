# Solana Devnet Airdrop Test

Small test repository for working with Solana devnet.

## Purpose

This repo is used to test:

- Solana CLI configuration
- Devnet wallet funding
- Balance checks
- Basic setup for local blockchain experiments

## Commands

Set Solana to devnet:

```bash
solana config set --url devnet
